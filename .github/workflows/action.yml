name: Changelog CI

on:
  pull_request:
    types: [opened]

  workflow_dispatch:
    inputs:
      release_version:
        description: 'Set Release Version'
        required: true

  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Obtener versión para Changelog
        id: version_step
        run: |
          if [ -z "${{ github.event.inputs.release_version }}" ]; then
            VERSION=$(cat version.txt)
          else
            VERSION="${{ github.event.inputs.release_version }}"
          fi

          # Limpiar caracteres especiales del nombre de la versión
          VERSION_CLEAN=$(echo "$VERSION" | tr -cd '[:alnum:]._-')
          echo "release_version=$VERSION_CLEAN" >> $GITHUB_OUTPUT

      - name: Run Changelog CI
        uses: saadmk11/changelog-ci@v1.1.2
        with:
          changelog_filename: CHANGELOG.md
          config_file: .changelog-config.yml
          committer_username: 'github-actions[bot]'
          committer_email: 'github-actions[bot]@users.noreply.github.com'
          release_version: ${{ steps.version_step.outputs.release_version }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

